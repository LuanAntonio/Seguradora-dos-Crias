<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Consultor</title>
    <%- include("../partials/cabecalho.ejs") %>
</head>
<body>
    <h1>Editar Consultor</h1>
    <form action="/consultores/edit/<%= consultor._id %>" method="POST">
        <div>
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" value="<%= consultor.nome %>" required>
        </div>
        <div>
            <label for="idade">Idade:</label>
            <input type="number" id="idade" name="idade" value="<%= consultor.idade %>" required>
        </div>
        <div>
            <label for="email">E-mail:</label>
            <input type="email" id="email" name="email" value="<%= consultor.email %>" required>
        </div>
        <button type="submit">Salvar</button>
    </form>
    <a href="/consultores">Voltar para a lista de Consultores</a>
    <%- include("../partials/rodape.ejs") %>
</body>
</html>
