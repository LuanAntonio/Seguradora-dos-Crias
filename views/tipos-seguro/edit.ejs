<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Tipo de Seguro</title>
    <%- include("../partials/cabecalho.ejs") %>
</head>
<body>
    <h1>Editar Tipo de Seguro</h1>
    <form action="/tipos-seguro/edit/<%= tipoSeguro._id %>" method="POST">
        <div>
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" value="<%= tipoSeguro.nome %>" required>
        </div>
        <div>
            <label for="descricao">Descrição:</label>
            <textarea id="descricao" name="descricao" required><%= tipoSeguro.descricao %></textarea>
        </div>
        <div>
            <label for="preco">Preço:</label>
            <input type="number" id="preco" name="preco" value="<%= tipoSeguro.preco %>" step="0.01" required>
        </div>
        <button type="submit">Salvar</button>
    </form>
    <a href="/tipos-seguro">Voltar para a lista de Tipos de Seguro</a>
    <%- include("../partials/rodape.ejs") %>
</body>
</html>

